# S05: E2E test infrastructure — UAT

**Milestone:** M005
**Written:** 2026-03-25T14:47:12.386Z

# S05 UAT: E2E Test Infrastructure\n\n## Preconditions\n- Node.js installed, `npm install` completed\n- No process running on port 1420\n- Chromium browser installed via Playwright (`npx playwright install chromium`)\n\n## Test Cases\n\n### TC1: Full E2E suite passes\n1. Run `npx playwright test --reporter=list`\n2. **Expected:** All 22 tests pass, exit code 0\n3. **Expected:** Output shows tests from 8 spec files\n\n### TC2: Smoke test verifies infrastructure\n1. Run `npx playwright test e2e/smoke.spec.ts --reporter=list`\n2. **Expected:** 1 test passes — app loads and shell is visible\n\n### TC3: Sidebar navigation coverage\n1. Run `npx playwright test e2e/sidebar-navigation.spec.ts --reporter=list`\n2. **Expected:** 7 tests pass — one for each sidebar route (Chat, Projects, Milestones, Timeline, Costs, Settings, Pro Tools)\n3. Each test clicks a sidebar link and verifies the correct heading appears\n\n### TC4: Keyboard shortcuts\n1. Run `npx playwright test e2e/keyboard-shortcuts.spec.ts --reporter=list`\n2. **Expected:** 7 tests pass — Ctrl+1 through Ctrl+7 navigate to correct views\n3. Each test verifies URL change after shortcut\n\n### TC5: Pro Tools grid and panel navigation\n1. Run `npx playwright test e2e/pro-tools.spec.ts --reporter=list`\n2. **Expected:** 2 tests pass — grid renders categories, clicking a card navigates to panel\n\n### TC6: Deep-link routing\n1. Run `npx playwright test e2e/routing.spec.ts --reporter=list`\n2. **Expected:** 2 tests pass — direct URL to pro-tool panel works, unknown route doesn't crash\n\n### TC7: Toast notification\n1. Run `npx playwright test e2e/toast-notifications.spec.ts --reporter=list`\n2. **Expected:** 1 test passes — error toast appears when GSD store error is set\n\n### TC8: Settings page\n1. Run `npx playwright test e2e/settings.spec.ts --reporter=list`\n2. **Expected:** 1 test passes — settings heading and config panel render\n\n### TC9: Dev server auto-start\n1. Ensure no Vite dev server is running on port 1420\n2. Run `npx playwright test e2e/smoke.spec.ts`\n3. **Expected:** Playwright auto-starts the Vite dev server, test passes without manual server startup\n\n## Edge Cases\n- Running tests with port 1420 already in use: Playwright should reuse the existing server (reuseExistingServer: true in config)\n- Running individual spec files: Each should work independently without requiring other specs"
